# DDL_LOG

Every production schema or data application made under the **standing DDL authorisation**
(NOW.md, granted 2026-08-16 by SB on CD's advice) appends one line here.

**The log is what makes the grant oversight rather than a blank cheque.** SB reviews the list, not
each item — asynchronous control, same scrutiny, none of the interruptions.

**Record:** date · migration section (or "data") · what it did · which of the three conditions it
met (1 shape · 2 data disposition · 3 reversible) — or, for anything outside the grant, **who
authorised it and when**.

Newest last.

---

| Date | Section | What | Conditions / authorisation |
|---|---|---|---|
| 2026-08-16 | `010 §51` | `tenant_portal_tokens.communication_log_id` — nullable uuid FK to `communication_log`, plus a partial index `WHERE NOT revoked`. Binds a portal token to the comm that delivered it, so a hard bounce revokes exactly the credential that reached nobody (ADDENDUM_62F §17.1). | **1** nullable column + index · **2** no PII (a FK to a log row; the token table already has RLS and org scoping) · **3** revertible — dropping it loses only the binding, and `expires_at` remains the backstop. **Logged retroactively**: applied on Stéan's explicit ask, hours before the standing grant existed. Meets all three. |
| 2026-08-17 | data | **Backfill** — inserted 34 `contact_emails` rows from `contacts.primary_email` where no primary row existed (`is_primary`/`is_active` true, `email_type` `'personal'`). Step 1 of `ADDENDUM_CONTACT_REPRESENTATION_UNIFICATION` §7. | **OUTSIDE the standing grant** — a data operation, not a shape change. **Explicitly authorised by SB 2026-08-17.** Ran under CD §10.4's two conditions: dry-run count named in advance as **34** and matched exactly before executing, and a **single transaction** with post-conditions asserted inside it. Verified after: 34 total rows · 34 primary+active · **B = 0** divergence · **0** contacts remaining unbackfilled. INSERT-shaped — no existing row modified. |
| 2026-08-17 | `002 §22` | **Derive trigger + trap-4 CHECK** — `contacts.primary_email` becomes a derived cache of the single `(is_primary AND is_active)` `contact_emails` row, maintained by an AFTER INSERT/UPDATE/DELETE trigger; plus `contact_emails_primary_implies_active` CHECK. Step 3 of `ADDENDUM_CONTACT_REPRESENTATION_UNIFICATION` §7. | **OUTSIDE the standing grant** — a trigger and a constraint on a table with live write paths, explicitly the "always ask" shape. **Authorised by SB 2026-08-17.** Safe *because of the ordering*: steps 1–2 already backfilled 34 rows and made every writer dual-write, so the trigger is a no-op on landing — the original build order had it first, which would have silently broken agent contact edits. CHECK applied while free (34 rows, all compliant, no code path yet sets `is_active=false`). **PROBE-FIRED against production in a rolled-back transaction, all five arms:** update→cache follows · delete→cache NULL (the `anonymisePlan` arm) · insert→repopulates · CHECK blocks primary+inactive · CHECK actually raises. Drift clean, 1290/1290. |
| 2026-08-17 | data | **Backfill** — inserted 35 `contact_phones` rows from `contacts.primary_phone` (`is_primary`/`is_active` true, `phone_type` `mobile`). Step 1 of the `primary_phone` unification (NOW.md item 12) — the exact mirror of the `primary_email` backfill above. | **OUTSIDE the standing grant** — a data operation, not a shape change. **Authorised by SB 2026-08-17.** Same two conditions honoured: dry-run count named in advance as **35** and matched exactly before executing, and a **single transaction** with post-conditions asserted inside it. Verified after: 35 total rows · 35 primary+active · **B = 0** divergence · **0** remaining. INSERT-shaped — no existing row modified. `contact_phones` was empty (0 rows) beforehand, so there was no survivorship decision. |
| 2026-08-17 | `002 §23` | **Derive trigger + trap-4 CHECK, phone edition** — `contacts.primary_phone` becomes a derived cache of the single `(is_primary AND is_active)` `contact_phones` row via an AFTER INSERT/UPDATE/DELETE trigger; plus `contact_phones_primary_implies_active` CHECK. Step 3 of NOW.md item 12 — the mirror of `002 §22`. | **OUTSIDE the standing grant** — a trigger and a constraint on a table with live write paths, explicitly the "always ask" shape. **Authorised by SB 2026-08-17.** Safe *because of the ordering*: step 1 backfilled the 35 rows and step 2 (#246) routed all 24 writer sites through `syncPrimaryContactPhone`. **Proven a no-op before applying, not asserted:** a pre-flight query returned `would_change_on_derive = 0` — every contact's `primary_phone` already equalled what the derive function computes, platform-wide. CHECK applied FIRST (validating against the 35 existing rows, 0 violations) and while still free — no code path sets `is_active=false` yet. **PROBE-FIRED against production, all five arms, inside a block terminated by `RAISE EXCEPTION` so the rollback was guaranteed by the exception rather than by an assumption about transaction wrapping:** INSERT→populates · UPDATE→follows · CHECK raises `check_violation` naming the right constraint · DELETE→NULL (the `anonymisePlan` arm) · correct removal frees the slot. Post-verified: **0** probe rows survived, 35 rows unchanged, divergence **0**. Manifest regenerated (picked up exactly the function + CHECK, nothing else), drift clean, `check` 1290/1290, `test:db` 163 passed. |
