/**
 * app/(dashboard)/landlords/loading.tsx — route skeleton for /landlords
 *
 * Route:  /landlords
 * Notes:  Mirrors the page template (ResourcePageHeader + list) so there's no layout jump.
 */
import { PageSkeleton } from "@/components/ui/page-skeleton"

export default function LandlordsLoading() {
  return <PageSkeleton />
}
