/**
 * lib/extraction/fraudSignals.ts — Fraud signal detection (Phase 3 stub)
 *
 * Phase 3 will detect fraud signals (template reuse, metadata inconsistencies, etc.).
 * Spec: ADDENDUM_14L — Phase 3
 */

export function detectFraudSignals(): never {
  throw new Error("Fraud signal detection not implemented — Phase 3")
}
