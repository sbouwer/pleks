/**
 * lib/extraction/reconciler.ts — Cross-document field reconciliation (Phase 2 stub)
 *
 * Phase 2 will reconcile extracted fields across documents (income, name, ID number).
 * Spec: ADDENDUM_14L — Phase 2
 */

export function reconcile(): never {
  throw new Error("Reconciler not implemented — Phase 2")
}
